model: claude-sonnet-5

confidence_threshold: 0.75

match:
  tolerance_pct: 0.02
  tolerance_abs: 5.00

approval:
  auto_review_threshold: 1000.00

paths:
  inbox: inbox
  processed: processed
  output: output
  data: data
  state: state
